import 'package:flutter/material.dart';

class ObstacleListPage extends StatelessWidget {
  const ObstacleListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
